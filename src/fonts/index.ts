import { Roboto_Condensed, Viga } from 'next/font/google';

export const robotoCondensed = Roboto_Condensed({
  subsets: ['latin'],
  weight: ['400', '700'],
});

export const viga = Viga({
  subsets: ['latin'],
  weight: ['400'],
});
